enum ThemeModeType { Light, Dark }

enum ApiResult { Success, Error }

enum Action { CHECKIN, CHECKOUT }

enum HomeMenus { Absence, Late, Overtime, Leave, Help, Payroll, Report, Task, Announcement, Reimbursement, Setting }

enum AttendanceType { OnTime, Late }

enum ReimbursementStatus { All, Pending, Approved, Rejected }

enum TransportStatus { All, HistoryLoan, Approval, Driver }

enum InventoryStatus { All, RequestLoan, HandOver, HistoryLead }

enum ProfileMenuType { PersonalInformation, EmployeeInformation, Inventory, Password, Feedback, Rate, TermsCondition, Logout, Announcement }

enum SnackType { Success, Info, Error, Warning }

enum Status { Complete, Waiting, Rejected, Approved, HandOver, Expired }

enum BaseUrl { PRODUCTION, ABSENCE, STAGGING }

enum HealthSafetyEnvironmentStatus { All, RequestPinjam, HandOver, RequestExtend, HistoryLead }

enum DrawingStatus { All, History, Approval, Technician }

enum TestingTerminationStatus { History, Approval, Technician }
